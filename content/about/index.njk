---
title: About
layout: layouts/base.njk
eleventyNavigation:
  key: About
  order: 2
---

<p class="mb-8 text-center italic">
Software Engineer &amp; Product Owner at Dynatrace, developing Kubernetes Operator to simplify deployment of observability components on Kubernetes clusters.
</p>

<section class="mb-8">
  <h1 class="text-3xl font-extrabold">Experience</h1>
  <ul>
  {%- for experience in cv.experiences -%}
    <li class="mb-8">
      <p class="flex justify-between">
        <span>
          {{ experience.title }}
          at
          <a href="{{ experience.company.url }}" class="underline hover:decoration-pink-500">
            {{ experience.company.name }}
          </a>
        </span>
        <span>
          {%- if experience.to -%}
          {{ experience.from }}
          <span> - </span>
          {{ experience.to }}
          {%- else -%}
          from {{ experience.from }}
          {%- endif -%}
        </span>
      </p>
      <p>
        {% markdown %}{{ experience.description }}{% endmarkdown %}
      </p>
      <ul class="flex flex-row text-slate-400">
        {%- for technology in experience.technologies -%}
        <li class="p-1 mr-1">
          #{{technology}}
        </li>
        {%- endfor -%}
      </ul>
    </li>
  {%- endfor -%}
  </ul>
</section>

<section class="mb-8">
  <h2 class="text-3xl font-extrabold">Education</h2>
  <ul>
  {%- for education in cv.educations -%}
    <li class="flex justify-between">
      <span>
        {{ education.location }}-{{ education.degree }}
      </span>
    
      <span>
        {{ education.from }}-{{ education.to }}
      </span>
    </li>
  {%- endfor -%}
  </ul>
</section>